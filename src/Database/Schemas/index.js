const { clientModel } = require('./Client.js');
const { guildModel } = require('./Guild.js');
const { userModel } = require('./User.js');
const { commandModel } = require('./Command.js');

module.exports = {
    clientModel,
    guildModel,
    userModel,
    commandModel
};